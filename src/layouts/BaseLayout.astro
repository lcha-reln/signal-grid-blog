---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE, sitePath } from "../config";
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  article?: boolean;
}

const {
  title = SITE.title,
  description = SITE.description,
  canonical = Astro.url.toString(),
  article = false,
} = Astro.props;
const pageTitle = title === SITE.title ? title : `${title} · ${SITE.shortTitle}`;
const socialImage = new URL(sitePath("images/signal-grid-og.jpg"), SITE.url).toString();
---

<!doctype html>
<html lang={SITE.locale} data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#080b12" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href={sitePath("favicon.svg")} />
    <link rel="alternate" type="application/rss+xml" title={SITE.title} href={sitePath("rss.xml")} />
    <meta property="og:type" content={article ? "article" : "website"} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Signal Grid 抽象分布式节点与信号网络" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={socialImage} />
    <title>{pageTitle}</title>
    <script is:inline>
      (() => {
        const saved = localStorage.getItem("signal-grid-theme");
        const theme = saved || (matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark");
        document.documentElement.dataset.theme = theme;
      })();
    </script>
  </head>
  <body>
    <a class="skip-link" href="#main-content">跳到正文</a>
    <div class="ambient ambient-one" aria-hidden="true"></div>
    <div class="ambient ambient-two" aria-hidden="true"></div>
    <Header />
    <main id="main-content" data-pagefind-body={article ? true : undefined}>
      <slot />
    </main>
    <Footer />
  </body>
</html>
