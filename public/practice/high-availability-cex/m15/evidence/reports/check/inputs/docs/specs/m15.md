# M15 — Release operational qualification, PLAN 0.18

This contract adds operational qualification to the published M14 runtime. Its normative
inputs are the byte-frozen `profile-o1.json`, `arrivals-v1.json`, `workload-v1.json`,
N-1 binary fixture, this document, `m15-process-protocol.md` and
`m15-bundle-backup-protocol.md`. The start implements only the honest structured RED entry.
No operational PASS or product release is implied by the presence of this contract.

M00-M14 contracts, fixed references and published evidence remain immutable. All existing
matching-core, matching-local-runtime, matching-cluster-runtime and matching-reference
production trees remain byte-identical to M14 completion; M15 adds an external operations
application and qualification tools. Product composition may change while business component
bytes remain identical. The course still has 16 Matching / 33 SPOT units and creates no Counter/REST repo.

## Identity and release
- Unit M15, title 发布制品运行资格, PLAN0.18. Start course/m15-start; completion course/m15-complete; product matching-1.0.0. Code branch unit/m15.
- N-1 is course/m14-complete commit56c4ec09ddf9fcf57f1cce763ae8b451ba7f394f. Freeze complete11-jar6350332-byte fixture from the N-1 fixture manifest, original classes and Maven dependency bytes, with provenance and license attribution. Jar0.8.0 name is not matching-0.8.0 product (M12).
- N is clean current M15 Gradle installDist operations bundle, source commit+whole ordered classpath+role entrypoints+hashes measured at completion. Keep appVersion0x040000 and all M14 wire/state/business semantics. Same business component hashes allowed; no invented migration. Final bundle includes actual new operations capability.
- Separate version-neutral lifecycle observer tool artifact/hash for N-1 permitted, calls actual selected M14ClusterMember API and no substitute business classes. Record actual class code sources in child. Never leak current business jars into N-1 child. M14 originalMain hook alone cannot prove close completion.
- Supported qualification single host: macOS/aarch64 or Linux/amd64, Java25, >=4 logical CPUs, >=8GiB RAM; actual environment/VM/provider/filesystem recorded. Operational child JVM -Xms32m -Xmx512m plus required Aeron opens. OS-default Aeron receive buffer override may be used but actual flags recorded; no MTU/Q1 weakening.

## Ops, stop and backup bounds
- Add external matching-operations application after start; no service callback I/O. Provide executable verify-bundle, member, publisher, sink, backup, restore, inspect commands and documented explicit config. A new whole-fleet service manager is not required; role processes can be launched by runbook/controller with explicit ownership.
- Read-only bundle/config/backup preflight before existing member/sink entrypoints. Expected manifest SHA supplied separately; exact inventory/order/hash; safe paths/no symlinks/external marks/.lnk; copy hardlinks to independent bytes. Revalidate isolated bytes before install/use. Existing nonempty target refused. Staging failures never overwrite live roots.
- Owned EOF-driven lifecycle observer records successful component close completion, process exit0 and no force. 30s normal stop deadline per role, 90s all-role barrier; failure then owned force cleanup is allowed only with failed qualification. Role startup60s; scenario600s. All original roles stopped before switching artifactvector/copy.
- Backup is stopped vector of6 member identities (two3-voter groups), each COMPLETE archive/cluster tree incl catalog/segments/recording.log/node-state/marks; original bootstrap route+profile+genesis+importedM13snapshot; sink journal + sink identity and independently captured state/id/ownership/output cut. Preserve original bootstrap vs active route distinction.
- Frozen backup envelope: max512 logical files, max256MiB each, max16GiB total logical; lossless gzip per logical file, <=32MiB each compressed and <=128MiB total compressed; streaming buffer65536B. Separate manifest expectedSHA. Complete current bytes preserved; compressed bytes retained as public evidence (not hash-only). Canonical gzip single-member, strict header/CRC/size/EOF validation. No original data substituted by diagnostic snapshots.
- Empty new absolute root, same ports/IDs, freshStart=false; original runtime source unavailable (rename quarantine and prevent configured source path use) during actual restore; exact input paths rebound. No cross-host, changed-port, power-loss, rolling/mixed-version, online migration claims. Mark inactivity must be actually observed, not assumed from delay.
- N-1 establishes nonempty resting liquidity, >=16 real cross-account trades/shard, old retry identities, one business config change, nonzero ACK+grant, Market retention gap, snapshot+suffix, and8 pendingExecution/shard at selected cut. N restores, writes >=4 trades/shard then snapshot and2 suffix trades/shard. N-1 reopens CURRENT N roots (not oldbackup), preserves these full responses/output and writes >=2 newtrades/shard. New-root restore compares full cut before new ADMIN/control mutations, then actual output reconciliation and new business.
- The companion process protocol freezes the exact existing-command program. Its upgrade/backup maker has1000lots; the independent capacity fixture uses1000000lots. The named N-1 cut isE23/M22, Execution ACK15, durable sinkE16, pendingE16..E23. Its backup intentionally represents that named cut. Suffix-preserving rollback instead reopens CURRENT N data, and must not substitute that older backup.

## Capacity O1
- Two shards (BTC-USDT1,SOL-USDT2),6 N members,2 separate N publisher JVMs,1 N sink,independent controller. Keep existing full-state diagnostics enabled and disclosed; no fake pruning/historyguard change. Capacity input and publisher/reassembly queues specifically bounded; not global constantmemory claim.
- One independent schedule producer, one synchronous single-owner client worker/shard, each queue64+one worker-held item. Continuous bounded external publisher pump, no hidden unbounded arrival/retry queue.
- Preload valid SELL maker liquidity1000000 lots at price100 per selectedinstrument, opaque maker account different from IOCBUY taker account. Each logical arrival unique deterministic command and producer IDs (producer epoch1/sequence1); orderId1000000+ordinal. Logical input fixture frozen input contains, seed6515. All new admissions must be actual cross-account trades; full correlated response and output checked against independent expected model.
- Global rates, alternating shard1/shard2 by ordinal, offsets floor(stageIndex*1e9/rate), interval[0,10s): warmup25/s*10s=250; measured25/s*10s=250,100/s*10s=1000,250/s*10s=2500; then2000 same-planned-time burst. Baseline6000 including250 warmup. Explicit worker gate during burst schedule then release, to observe actual queue64 full. Record schedulerlateness not change schedule.
- Additional fault stage50/s*5s=250; total6250 declared arrivals. Kill only currently observed leader after20 correlated new business responses in that stage; arrivalproducer continues independent. Record failure/recovery/firstconfirmedpostkill at controller monotonic times. Report actual UNKNOWN count (zero allowed, no new UNKNOWNcoverageclaim ifzero); inherited M14 actual UNKNOWNwitnesses remain distinct.
- After each arrivalstage, drain/reconcile <=120s, maximum3 sameidentity reconciliation attempts per UNKNOWN; unresolvedUNKNOWN afterbound fails. Newcapacityrefusals may occur at any stage and stay separate; minimum each measured nominalstage >=10 actual new cross-account trades and durableoutput per shard. Burst must yield >=1 queuefull and observedqueue64; no throughput threshold or universalTPS guarantee.
- Separate C03 publisher1pause: fill32Execution actualbatches, get unchanged-state OUTPUT_BACKPRESSURED for one sameidentity; in samewindow shard2 >=4actualtrades+durableoutput; resume boundedpump, drain, retryoriginalrefusedidentity firstAPPLIED then exactreplay. Prelude, control, retries, overload inputs are separate actualattempt counts, never relabelled as6250arrivals.
- Main latency plannedarrival->correlatedresponse; also dispatch/offer/response/sink durable/replicatedACK controllerobservations separately, each metric sampledefinition+count. Never subtract nanoTime fromdifferentJVMs. Keep logicalledger+primitiveattemptledger, all plannedordinals unique and complete, UNKNOWNoriginalclassification retained alongside resolution. Validator recomputesbalances fromraw trace.
- Actual perchild resources at1s outsidecallbacks: role/PID,heapused/committed/max,GCcount/time,processCPU,directbuffers required; FD optional explicitunavailable; runtime directories logical bytes,diagnostics,sinkjournal,retainedhistory and snapshot growth. At least3 actualsamples per role in nominalload. Componenterrors fail rather than become overload. Actualprocess IDs/teardown receipts preserved.
- C05 finitehistory snapshot+real restart/cut equality after load; retained identities and output/cursors not pruned; observed recovery time only. Faultstage does not replace this. The killed C04 launch remains an explicit EXPECTED_FAULT; its replacement must rejoin all six members before C05, as frozen in the process protocol.
- C06 accounting validator controls are fixed in `validator-controls-v1.json`. They are explicitly synthetic finite accounting inputs, each required to pass before its declared mutation is actually rejected as STUDENT_FAILURE. They never count as operational arrivals, actual attempts, trades or new UNKNOWN witnesses. UNKNOWN and OUTPUT_PROFILE_LIMIT may be absent from the real capacity run; when present, additionally mutate the actual raw ledger. All other C06 mutations operate on this run's real raw ledgers. The validator preserves the primitive attempt facts when checking the changed logical classification; unexpected parsing or IO is SYSTEM_ERROR, not a semantic rejection.

## Qualification evidence
- Frozen finite reviewcase families V01-V04,B01-B06,R01-R06,C01-C06 (22 aggregatecaseIDs with each declared variant), per the frozen workload case/variant list and the two companion protocols. All positive roles real processes; negative bundle/archive/ledger perturbations actually executed, full before/after inventory/zero-child assertions where applicable. Do not count SYSTEM_ERROR as expected rejection or kill.
- Fresh current-source M14 qualification (including fresh M13) inherited, exact frozenbytes and matching-core proof. M15 local correctness/validator tests plus real operationalcases. Always preserve original producedbytes beforecopy and copy/install revalidation; rawbackup/releasebinarypayloads available. Overall M15 evidence <=512 artifacts, <=384MiB total, <=32MiB individual artifact (manifest separate); strict actual inventory/hash, clean completion+product tags bothsameHEAD required forfinalexport.
- Five exact lesson titles/permalinks/order10..50 from the lesson table below. Contractstage no lessonMarkdown yet. PLAN/config/units synced; unitREADY onlyafterstartGREEN/RED; no publicationuntil clean completeCI/freshmanifest/code+producttag/5tutorials/fullbloggate/Pages/livehash+browser.

## Immutable lesson identities

| Order | Title | Permalink |
| --- | --- | --- |
| 10 | M15·01：文件名相同，运行的还是同一套撮合制品吗 | release-bundle-identity-and-compatibility |
| 20 | M15·02：升级后已经成交，旧版本还能安全接回来吗 | stopped-upgrade-and-suffix-preserving-rollback |
| 30 | M15·03：原目录已经不可用，冷备还能够恢复什么 | cold-backup-and-relocated-restore |
| 40 | M15·04：负载超过承受范围时，怎样知道系统没有假装成功 | cluster-capacity-overload-and-diagnostics |
| 50 | M15·05：把部署、停止与恢复承诺交付为 matching-1.0.0 | operational-runbook-and-matching-one-release |

## Start and completion gates

At exact annotated `course/m15-start`, run `./gradlew clean build`, then a fresh
`./gradlew m14Check`; both must be GREEN. `./gradlew m15Check` must exit1 with
`matching.m15.check.v1`, `GOAL_NOT_IMPLEMENTED`, `executed=false`, all actual process
counts0, declared22 cases and6250 arrivals, and no actual product release. Only this
observed start authorizes implementation. Future report schema v2 carries actual operational
results; it must preserve these frozen obligations, and unexpected IO/parser/process exceptions
are SYSTEM_ERROR, never an expected negative or mutant kill.

At completion, clean build and fresh cumulative M15 qualification must pass locally and in CI.
Both annotated `course/m15-complete` and `matching-1.0.0` must point to the exact clean source
before `m15Evidence` freshly executes and exports. Published evidence contains the original
runtime bundle bytes, full recoverable compressed backup bytes, raw logical/attempt ledgers,
per-process lifecycle/resource/diagnostic records and inherited fresh M14 evidence. The finite
case IDs and all109 named variants are obligations, not an assertion of exhaustive coverage.

The website publishes all five lessons only after the complete code and product tags, independent
clean qualification, original-byte evidence registration and full site gate pass. Pages must then
serve every manifest-bound payload with the correct hash and working lesson navigation/search.
